{
    "type": "object",
    "properties": {
        "from": {
            "required": true,
            "type": "string",
            "format": "date-time"
        },
        "to": {
            "required": true,
            "type": "string",
            "format": "date-time"
        },
        "limit": {
            "maximum": 5000,
            "minimum": 1,
            "type": "integer",
            "format": "int64"
        },
        "offset": {
            "minimum": 0,
            "type": "integer",
            "format": "int64"
        },
        "org_id": {
            "required": true,
            "type": "string"
        }
    }
}